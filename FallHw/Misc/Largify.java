public class Largify {
    public static void Largify(int[] shorter. int n) {
	int[] larger = new  int[shorter.length + n];
	for (int x = 0; x < shorter.length; x++) {
	    larger[x] = shorter[x];
	}
    }
}
